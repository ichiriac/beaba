This folder will contains gettext translation files for each country.

To enable a new language, take a look at ./config/infos.php :

````php
<?php
return array(
    ...
    'langs'         => 'en,fr'
    ...
);
```